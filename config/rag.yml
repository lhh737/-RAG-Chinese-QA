# RAG 模型配置（嵌入用 DashScope text-embedding-v4 API，重排序本地加载）
reranker_model_name: BAAI/bge-reranker-base

# 生成参数（Qwen API，见 generator.py）
max_new_tokens: 512
temperature: 0.2
top_p: 0.9

# 设备（仅对本地模型有效，如 reranker）：cuda / cpu / auto
torch_device: auto
